import 'package:flutter/material.dart';

class GunsBuildFragment extends StatelessWidget {
  const GunsBuildFragment({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}