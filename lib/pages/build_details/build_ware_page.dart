import 'package:flutter/material.dart';

class WareBuildFragment extends StatelessWidget {
  const WareBuildFragment({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}